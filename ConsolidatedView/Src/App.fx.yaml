App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =ClearCollect(appScreens, {Name: "Permissions", Screen: PermissionsPage}, {Name: "Authenticate", Screen: AuthenticateQuestionsPage}, {Name: "Business Detail", Screen: BusinessDetailsPage}, {Name: "Land Parcel", Screen: LandParcelPage});
        Set(graphQlUrl, "https://f311-145-224-66-97.ngrok-free.app/");
        Set(customerId, "123");
        Set(
            customerBusinesses,
            customer_businesses.Run("123", graphQlUrl).data.customer.businesses
        );
        Set(activeBusiness, First(customerBusinesses));
    StartScreen: =AuthenticateQuestionsPage
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

